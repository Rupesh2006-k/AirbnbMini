<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Listing</title>
    <link rel="stylesheet" href="/styleSheets/edit.css" />
  </head>
  <body>
    <div class="container">
      <h1>Edit Your Listing</h1>

      <form autocomplete="off" action="/listings/<%=leastingEdit._id%>?_method=PUT" method="post" class="form">

        <label>Title</label>
        <input type="text" name="title" value="<%= leastingEdit.title %>" required />

        <label>Description</label>
        <textarea name="description" required><%= leastingEdit.description %></textarea>

        <label>Price</label>
        <input type="number" name="price" value="<%= leastingEdit.price %>" required />

        <label>Location</label>
        <input type="text" name="location" value="<%= leastingEdit.location %>" required />

        <label>Country</label>
        <input type="text" name="country" value="<%= leastingEdit.country %>" required />

        <button type="submit" class="btn">Save Changes</button>
      </form>
    </div>
  </body>
</html>
