<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Listing</title>
    <link rel="stylesheet" href="/styleSheets/edit.css" />
  </head>
  <body>
    <div class="container">
      <h1>Edit Your Listing</h1>

      <form autocomplete="off" action="/listings/<%= listingEdit._id %>?_method=PUT" method="post" class="form">

        <label>Title</label>
        <input type="text" name="listing[title]" value="<%= listingEdit.title %>" required />

        <label>Description</label>
        <textarea name="listing[description]" required><%= listingEdit.description %></textarea>

        <label>Price</label>
        <input type="number" name="listing[price]" value="<%= listingEdit.price %>" required />

        <label>Location</label>
        <input type="text" name="listing[location]" value="<%= listingEdit.location %>" required />

        <label>Country</label>
        <input type="text" name="listing[country]" value="<%= listingEdit.country %>" required />

        <button type="submit" class="btn">Save Changes</button>
      </form>
    </div>
  </body>
</html>
